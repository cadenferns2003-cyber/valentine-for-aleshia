<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Be My Valentine 💜</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #5a189a, #c77dff);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      font-family: 'Comic Sans MS', cursive, sans-serif;
    }

    .card {
      background: #f8f0ff;
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
      position: relative;
      width: 340px;
      z-index: 2;
    }

    h1 {
      color: #5a189a;
      margin-bottom: 20px;
    }

    p {
      color: #3c096c;
      font-size: 16px;
      margin-top: 20px;
      opacity: 0;
      transform: translateY(10px);
      transition: opacity 1.5s ease, transform 1.5s ease;
    }

    p.show {
      opacity: 1;
      transform: translateY(0);
    }

    button {
      padding: 12px 25px;
      font-size: 18px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      position: absolute;
    }

    #yes {
      background-color: #9d4edd;
      color: white;
      left: 40px;
      bottom: 25px;
    }

    #no {
      background-color: #cdb4db;
      color: #3c096c;
      right: 40px;
      bottom: 25px;
    }

    /* Floating hearts */
    .heart {
      position: absolute;
      font-size: 22px;
      animation: float 6s linear infinite;
      color: rgba(255, 255, 255, 0.85);
    }

    @keyframes float {
      0% {
        transform: translateY(100vh);
        opacity: 1;
      }
      100% {
        transform: translateY(-10vh);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <div class="card" id="card">
    <h1 id="title">Aleshia 💜<br>Will you be my Valentine?</h1>

    <p id="hiddenMessage">
      Happy Valentine’s boo 💜<br><br>
      This is something small and special<br>
      for a very special woman.<br><br>
      Love you 💜
    </p>

    <button id="yes">Yes 💜</button>
    <button id="no">No 😏</button>
  </div>

  <!-- Background music -->
  <audio id="music" loop>
    <source src="https://cdn.pixabay.com/audio/2022/03/15/audio_4c7e8f4c65.mp3" type="audio/mpeg">
  </audio>

  <script>
    const noButton = document.getElementById("no");
    const yesButton = document.getElementById("yes");
    const card = document.getElementById("card");
    const hiddenMessage = document.getElementById("hiddenMessage");
    const title = document.getElementById("title");
    const music = document.getElementById("music");

    // No button dodges on hover
    noButton.addEventListener("mouseenter", () => {
      const cardRect = card.getBoundingClientRect();
      const btnRect = noButton.getBoundingClientRect();

      const maxX = cardRect.width - btnRect.width;
      const maxY = cardRect.height - btnRect.height;

      noButton.style.left = Math.random() * maxX + "px";
      noButton.style.top = Math.random() * maxY + "px";
    });

    // Yes button action
    yesButton.addEventListener("click", () => {
      title.innerHTML = "YAY ALESHIA 😍💜";
      hiddenMessage.classList.add("show");
      yesButton.style.display = "none";
      noButton.style.display = "none";
      music.play();
    });

    // Floating hearts
    function createHeart() {
      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.innerHTML = "💜";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (Math.random() * 3 + 4) + "s";
      document.body.appendChild(heart);

      setTimeout(() => heart.remove(), 6000);
    }

    setInterval(createHeart, 400);
  </script>

</body>
</html>
